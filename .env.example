# Which MOI network to talk to: voyage | mainnet | custom
MOI_NETWORK=voyage

# Only required when MOI_NETWORK=custom
# MOI_RPC_URL=https://...

# REQUIRED. WalletConnect project id — this is how your laptop and your phone
# find each other through the relay. It is a public identifier, not a secret.
#
# Get one free:
#   1. Sign in at https://cloud.reown.com
#   2. Create a project (any name; type "AppKit" is fine)
#   3. Copy the Project ID — 32 hex characters
#
# Reads work without it. Pairing and every write do not.
# TODO(rahul): a shared project id to ship as a package default (open question 6)
WC_PROJECT_ID=REPLACE_ME

# Where session.json and the WalletConnect keystore live
MOI_MCP_HOME=~/.moi-mcp

# Explorer base used to build explorerUrl in write results
# TODO: confirm canonical explorer host
MOI_EXPLORER_URL=https://voyage.moi.technology

# How long to wait for the user to approve on their phone (ms)
REQUEST_TIMEOUT_MS=300000

# silent | error | info | debug  — all logs go to stderr, never stdout
LOG_LEVEL=error
